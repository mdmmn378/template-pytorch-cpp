Template Pytorch C++ Extension Builder
---------------------------------------
Steps
- Create a new virtual env
- Required python version `python-3.9.12`
- Install requirements
    - `pip install -r requirements.txt`
    - `python setup.py install`
- Check installation
    - `pytest`